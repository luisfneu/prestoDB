CREATE SCHEMA hive.local
WITH (location = 'file:///hive/warehouse:/hive/data');



CREATE TABLE default.products (
    id VARCHAR,
    product_name VARCHAR,
    category VARCHAR,
    price VARCHAR,
    stock VARCHAR
)
WITH (
    format = 'CSV',
    external_location = 'file:///hive/warehouse:/hive/data'
);